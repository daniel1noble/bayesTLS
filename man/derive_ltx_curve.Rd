% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tdt.R
\name{derive_ltx_curve}
\alias{derive_ltx_curve}
\title{Posterior LT_x curve: time to reach a given survival target at each temperature}
\usage{
derive_ltx_curve(
  workflow,
  temp_grid,
  duration_grid = NULL,
  target_surv = 0.5,
  ndraws = 1000,
  probs = c(0.025, 0.5, 0.975),
  time_multiplier = 60,
  output_time_unit = "min"
)
}
\arguments{
\item{workflow}{Fitted \code{bayes_tls}.}

\item{temp_grid}{Numeric vector of temperatures (°C).}

\item{duration_grid}{Numeric vector of durations along which to search.
Default: 350 log-spaced values spanning 0.2× to 5×
the training data's duration range.}

\item{target_surv}{Survival probability to invert at. Default 0.5.}

\item{ndraws}{Posterior draws to use. Default 1000.}

\item{probs}{Quantile probabilities for the summary. Default
\code{c(0.025, 0.5, 0.975)}.}

\item{time_multiplier}{Multiplier from model time units to output time
units (e.g. 60 for hours → min). Default 60.}

\item{output_time_unit}{Label for the output time unit. Default \code{"min"}.}
}
\value{
A list with \code{draws} (per-draw threshold durations), \code{summary}
(quantile summary by temperature), \code{target_surv}, \code{time_multiplier},
\code{output_time_unit}.
}
\description{
For each posterior draw and each temperature in \code{temp_grid}, finds the
duration at which population-level survival crosses \code{target_surv}. The
inversion is numerical — predict survival on a dense duration grid, then
\code{approx()} through \code{target_surv}.
}
\details{
This is the \strong{horizontal} read of the survival surface: fix a survival
threshold, read off the time required to reach it at each temperature.
}
