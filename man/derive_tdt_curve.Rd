% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tdt.R
\name{derive_tdt_curve}
\alias{derive_tdt_curve}
\title{Posterior LT_x curve: time to reach a survival target at each temperature}
\usage{
derive_tdt_curve(
  workflow,
  temp_grid,
  duration_grid = NULL,
  target_surv = "relative",
  ndraws = 1000,
  probs = c(0.025, 0.5, 0.975),
  time_multiplier = NULL,
  output_time_unit = "min"
)
}
\arguments{
\item{workflow}{Fitted \code{bayes_tls}.}

\item{temp_grid}{Numeric vector of temperatures (°C).}

\item{duration_grid}{Numeric vector of durations along which to search.
Only used in \code{"absolute"} mode. Default: 350
log-spaced values spanning 0.2× to 5× the training
data's duration range.}

\item{target_surv}{Threshold mode. \code{"relative"} (default), \code{"absolute"}
(= 0.5), or a numeric in \verb{(0, 1)}.}

\item{ndraws}{Posterior draws to use. Default 1000.}

\item{probs}{Quantile probabilities for the summary. Default
\code{c(0.025, 0.5, 0.975)}.}

\item{time_multiplier}{Multiplier from model time units to \code{output_time_unit}
(e.g. 60 for an hours model → min). \code{NULL} (default)
derives it automatically from the workflow's
\code{duration_unit} and \code{output_time_unit}, so a minutes
model and an hours model both give the correct result
without manual tuning. Pass a value to override.}

\item{output_time_unit}{Label for the output time unit. Default \code{"min"}.}
}
\value{
A list with \code{draws} (per-draw threshold durations; \code{target_surv}
column is a character label), \code{summary} (quantile summary by
temperature), \code{target_surv} (the label), \code{time_multiplier},
\code{output_time_unit}.
}
\description{
Returns the per-draw duration at which population-level survival crosses
the chosen threshold, at each temperature in \code{temp_grid}.
}
\details{
Two threshold modes are supported via \code{target_surv}:
\itemize{
\item \code{"relative"} (default): the duration at which survival reaches the
midpoint between the fitted lower and upper asymptotes, i.e.
\code{(low + up)/2}. This is the 4PL \code{mid} parameter on the natural time
axis, returned directly from \code{posterior_linpred(nlpar = "mid")} — no
numerical inversion. When \verb{low ≈ 0} and \verb{up ≈ 1} it coincides with
the classical LT50.
\item \code{"absolute"} (or a numeric \code{p} in \verb{(0, 1)}): the duration at which
survival crosses the literal probability \code{p} (0.5 by default). The
inversion is numerical — predict survival on a dense duration grid,
then \code{approx()} through \code{p}.
}

This is the \strong{horizontal} read of the survival surface: fix a survival
threshold, read off the time required to reach it at each temperature.
}
