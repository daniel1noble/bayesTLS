% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tdt_landscape.R
\name{derive_tdt_landscape}
\alias{derive_tdt_landscape}
\title{Posterior survival landscape across the temperature × duration plane}
\usage{
derive_tdt_landscape(
  workflow,
  temp_grid = NULL,
  duration_grid = NULL,
  ndraws = NULL,
  probs = c(0.025, 0.5, 0.975)
)
}
\arguments{
\item{workflow}{Fitted \code{bayes_tls}.}

\item{temp_grid}{Numeric vector of temperatures (°C). Default: 120
equally spaced values across the training-data range.}

\item{duration_grid}{Numeric vector of durations. Default: 120 log-spaced
values across the training-data range.}

\item{ndraws}{Posterior draws to use; \code{NULL} (default) uses the full
posterior. Pass an integer to subsample for speed.}

\item{probs}{Quantile probabilities. Default \code{c(0.025, 0.5, 0.975)}.}
}
\value{
A list with the same shape as \code{\link[=predict_survival_curves]{predict_survival_curves()}} (a
\code{summary} tibble with \code{temp}, \code{duration}, \code{survival_median},
\code{survival_lower}, \code{survival_upper}, plus \code{draws_matrix} and \code{grid}).
}
\description{
Returns the posterior median (and 95\% credible band) of survival probability
on a 2-D grid covering the experimental range. Suitable for plotting as a
heatmap or contour with \code{\link[=plot_tdt_landscape]{plot_tdt_landscape()}} (Phase 1d). For curves at a
handful of temperatures, use \code{\link[=predict_survival_curves]{predict_survival_curves()}} instead.
}
\examples{
\dontrun{
wf  <- fit_4pl(d, ...)
lsp <- derive_tdt_landscape(wf)
lsp$summary
}
}
