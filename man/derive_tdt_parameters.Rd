% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tdt.R
\name{derive_tdt_parameters}
\alias{derive_tdt_parameters}
\title{Per-draw z and CTmax from an LT_x curve via per-draw log-linear regression}
\usage{
derive_tdt_parameters(ltx_curve, t_ref = 60, min_points = 5)
}
\arguments{
\item{ltx_curve}{Output of \code{\link[=derive_ltx_curve]{derive_ltx_curve()}}.}

\item{t_ref}{Reference time (in \code{output_time_unit} of \code{ltx_curve}) for
computing CTmax. Default 60 (i.e. CTmax_1hr in min units).}

\item{min_points}{Minimum number of temperatures a draw must have to be
included. Default 5.}
}
\value{
A list with \code{draws} (per-draw slope, z, CTmax, R²), \code{summary},
\code{t_ref}.
}
\description{
Bayesian analogue of fitting the classical TDT line. For each posterior
draw, regress \verb{log10(LT_x duration)} on temperature and read off the
classical TDT quantities:
}
\details{
\itemize{
\item \code{z = -1 / slope} — thermal sensitivity, in °C per decade of time
\item \code{CTmax = (log10(t_ref) - intercept) / slope} — temperature at which
LT_x equals the reference time.
}
}
