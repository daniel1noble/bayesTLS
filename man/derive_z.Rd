% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tdt.R
\name{derive_z}
\alias{derive_z}
\title{Per-draw thermal sensitivity z directly from the joint posterior}
\usage{
derive_z(
  workflow,
  target_surv = "relative",
  temp_grid = NULL,
  ndraws = NULL,
  probs = c(0.025, 0.5, 0.975),
  h = 0.001
)
}
\arguments{
\item{workflow}{Fitted \code{bayes_tls}.}

\item{target_surv}{Threshold mode: \code{"relative"} (default), \code{"absolute"}
(= 0.5), or a numeric in \verb{(0, 1)}.}

\item{temp_grid}{Temperatures at which to evaluate local z and over which
to pool. Default: the observed (unique) assay temperatures
— pooling only where the data inform the curve.}

\item{ndraws}{Posterior draws to subsample, or \code{NULL} (default) for all.}

\item{probs}{Quantile probabilities for the summaries. Default
\code{c(0.025, 0.5, 0.975)}.}

\item{h}{Temperature step (°C) for the central finite difference
used in absolute mode. Default \code{1e-3}.}
}
\value{
A list with:
\itemize{
\item \code{draws}: tibble \verb{(.draw, z)} — pooled per-draw z.
\item \code{summary}: tibble \verb{(z_median, z_lower, z_upper)}.
\item \code{local_draws}: tibble \verb{(.draw, temp, z)} — local z(T) per draw.
\item \code{local_summary}: tibble \verb{(temp, z_median, z_lower, z_upper)}.
\item \code{target_surv}, \code{temp_grid}.
}
}
\description{
Derives \eqn{z = -1 / (\mathrm{d}/\mathrm{d}T\,\log_{10}\mathrm{LT}(T))} per
posterior draw, read straight from the fitted 4PL coefficients — \strong{no
regression}. There are two regimes:
}
\details{
\itemize{
\item \strong{Relative threshold} (default; the \eqn{(\ell+u)/2} midpoint):
\eqn{\log_{10}\mathrm{LT}_{\text{rel}}(T) = \mathrm{mid}(T) =
  \beta_0 + \beta_1 (T-\bar T)} is exactly linear, so
\eqn{z = -1/\beta_1} where \eqn{\beta_1} is the temperature slope on
\code{mid} (\code{b_mid_temp_c}). The asymptotes \eqn{\ell, u} and slope \eqn{k}
do not enter (the midpoint cancels the curve asymmetry). z is constant in
temperature.
\item \strong{Absolute threshold} \eqn{p}: the LT curve gains the asymmetry-correction
term, \eqn{\log_{10}\mathrm{LT}_p(T) = \mathrm{mid}(T) +
  \tfrac{1}{k(T)}\log\tfrac{u(T)-p}{p-\ell(T)}}. When \eqn{\ell}, \eqn{u} or
\eqn{k} carry temperature effects this bends the curve, so z varies with
temperature. A \strong{local} \eqn{z(T) = -1/m(T)} is computed at each assay
temperature, where the local slope \eqn{m(T)} is obtained by a central
finite difference of the closed-form LT curve (step \code{h}). When the shape
parameters are constant in T the correction is flat and this reduces to
\eqn{-1/\beta_1}.
}

The returned \strong{pooled} z (the default single summary) is the per-draw mean
of the local \eqn{z(T)} over \code{temp_grid}. The full per-temperature local
\eqn{z(T)} is also returned. z is invariant to the time unit (a constant
time-multiplier shifts the LT intercept, not its slope), so no
\code{time_multiplier} is needed here.
}
\examples{
\dontrun{
wf <- fit_4pl(std)
z  <- derive_z(wf)             # relative: z = -1 / b_mid_temp_c per draw
z$summary
derive_z(wf, target_surv = "absolute")$local_summary  # local z(T)
}
}
