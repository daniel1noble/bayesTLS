% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_4pl.R
\name{fit_4pl}
\alias{fit_4pl}
\title{Fit the joint Bayesian 4PL to standardised TDT data}
\usage{
fit_4pl(
  data,
  random_effects = NULL,
  temp_effects = c("low", "up", "k", "mid"),
  lower = 0,
  upper = 1,
  family = NULL,
  prior = NULL,
  chains = 4,
  iter = 4000,
  warmup = floor(iter/2),
  cores = chains,
  seed = 123,
  backend = "cmdstanr",
  control = list(adapt_delta = 0.95, max_treedepth = 12),
  init = 0,
  file = NULL,
  file_refit = "on_change",
  fit = TRUE,
  bounds = NULL,
  ctmax = NULL,
  z = NULL,
  up = NULL,
  low = NULL,
  k = NULL,
  mid = NULL,
  by = NULL,
  threshold = c("relative", "absolute"),
  p = 0.5,
  t_ref = 60,
  ...
)
}
\arguments{
\item{data}{Output of \code{\link[=standardize_data]{standardize_data()}}.}

\item{random_effects}{Optional character vector of grouping variables for
random intercepts on \code{mid}.}

\item{temp_effects}{Which 4PL sub-parameters carry a \code{temp_c} slope; a subset
of \code{c("low", "up", "k", "mid")} (default all four).
\code{"mid"} is always required. The all-four default needs
roughly >= 15 cells per fixed effect to be stable; for
sparse designs (few temperatures x durations, small n)
prefer \code{temp_effects = "mid"} (constant-shape), which
estimates only the midpoint's temperature slope (= -1/z)
and shares the asymptotes and steepness across
temperature. Passed to \code{\link[=make_4pl_formula]{make_4pl_formula()}}.}

\item{lower, upper}{Response-scale bounds for the asymptotes. Default
\verb{0, 1}; pass \verb{lower = 0.85, upper = 1} for sublethal
data bounded above zero.}

\item{family}{brms family. \code{NULL} (default) picks the family from the
data's \code{response_type} metadata:
\code{beta_binomial(link = "identity")} for count data and
\code{brms::Beta(link = "identity")} for a continuous
\code{proportion} response. Pass an explicit family to
override (e.g. \code{binomial(link = "identity")} for the
no-overdispersion count case).}

\item{prior}{Optional \code{brmsprior} object. If \code{NULL} (default),
\code{\link[=make_4pl_priors]{make_4pl_priors()}} builds defaults from \code{data},
\code{lower}, \code{upper}, and \code{random_effects}.}

\item{chains, iter, warmup, cores, seed}{Sampling arguments passed to \code{\link[brms:brm]{brms::brm()}}.}

\item{backend}{Either \code{"cmdstanr"} (default) or \code{"rstan"}.}

\item{control}{List of HMC control parameters.}

\item{init}{Initial values for the sampler. Default \code{0} initialises
every unconstrained parameter at zero.}

\item{file}{Optional path (without extension) to cache the fit.}

\item{file_refit}{Passed to \code{\link[brms:brm]{brms::brm()}}. Default \code{"on_change"}.}

\item{fit}{Logical. If \code{FALSE}, returns the workflow spec without
fitting — useful for inspecting the formula and priors.}

\item{bounds}{Length-2 \code{c(lower, upper)} asymptote interval;
supersedes \code{lower}/\code{upper} when supplied.}

\item{ctmax, z, up, low, k}{One-sided formulas selecting the \strong{direct} CTmax/z
parameterisation (see \code{\link[=make_4pl_formula]{make_4pl_formula()}}). Supplying
\code{ctmax} and/or \code{z} fits CTmax and z directly; \code{up}/\code{low}/\code{k}
are inherited from them when omitted. \code{up}/\code{low}/\code{k} also
take explicit formulas in the \strong{midpoint} parameterisation.}

\item{mid, by}{\strong{Midpoint} parameterisation only (see
\code{\link[=make_4pl_formula]{make_4pl_formula()}}). \code{mid} is an explicit one-sided
formula for the midpoint (must carry \code{temp_c}); \code{by} is a
moderator column-name shortcut applying the moderator to
all four sub-parameters (\code{low}/\code{up}/\code{k}/\code{mid}) at once, so
a per-group CTmax/z fit needs only \code{by = "<moderator>"}.
Either records the moderator in \code{meta$group_vars}, so
\code{\link[=extract_tdt]{extract_tdt()}} / \code{\link[=tls]{tls()}} auto-derive per-group quantities
with \verb{by = }. Both error in direct mode.}

\item{threshold}{\code{"relative"} (default) or \code{"absolute"}; the threshold the
fitted CTmax/z refer to (direct mode only).}

\item{p}{Survival fraction for the \code{"absolute"} threshold (direct
mode). Default \code{0.5} (LT50). The disjoint-bounds reparam
splits the asymptotes at the bounds' midpoint, so an
absolute \code{p} is only achievable \emph{near that midpoint}
(with the default bounds \verb{[0, 1]} the achievable range is
roughly \verb{(0.499, 0.501)}); \code{fit_4pl()} errors otherwise.
For any other LTx (LT60, LT90, ...) or a sub-unit-bounded
response, fit with \code{threshold = "relative"} and derive it
afterwards via \code{\link[=extract_tdt]{extract_tdt()}}/\code{\link[=tls]{tls()}} with
\verb{target_surv = }.}

\item{t_ref}{Reference exposure for CTmax, in \strong{minutes} (default 60,
i.e. one hour). Converted to the model's log10-time scale
via the data's \code{duration_unit}.}

\item{...}{Further arguments passed to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
A list of class \code{"bayes_tls"} with elements \code{fit}, \code{data},
\code{formula}, \code{prior}, \code{meta}. See \code{\link[=print.bayes_tls]{print.bayes_tls()}},
\code{\link[=summary.bayes_tls]{summary.bayes_tls()}}, and \code{\link[=plot.bayes_tls]{plot.bayes_tls()}} for the available
methods.
}
\description{
Wraps \code{\link[=make_4pl_formula]{make_4pl_formula()}} + \code{\link[=make_4pl_priors]{make_4pl_priors()}} + \code{\link[brms:brm]{brms::brm()}}. Returns
a workflow object containing the fit, data, formula, prior, and metadata
that downstream helpers (e.g. \code{\link[=extract_tdt]{extract_tdt()}}) read from.
}
\details{
By default all four 4PL sub-parameters get a \code{temp_c} slope, and random
intercepts attach to \code{mid} only. Use \code{temp_effects = "mid"} for the classical
constant-shape TDT model (recommended for sparse designs — see \code{temp_effects}).
To fit separate categories (life stages, species, populations), filter \code{data}
per category and call this function once per subset.
}
\examples{
\dontrun{
d  <- standardize_data(raw, ...)
wf <- fit_4pl(d, fit = FALSE)         # inspect spec only
wf <- fit_4pl(d, file = "output/models/my_fit")
wf <- fit_4pl(d, lower = 0.85)        # sublethal data
}
}
