% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_twostage_helpers.R
\name{fit_joint_4pl_sim}
\alias{fit_joint_4pl_sim}
\title{Joint Bayesian 4PL fit + extract for one simulated dataset}
\usage{
fit_joint_4pl_sim(data, ndraws = 1000, chains = 2, iter = 2000, seed = 1L)
}
\arguments{
\item{data}{Output of \code{\link[=sim_twostage_dataset]{sim_twostage_dataset()}}.}

\item{ndraws}{Posterior draws to keep from \code{extract_tdt()}. Default 1000.}

\item{chains}{Number of MCMC chains. Default 2.}

\item{iter}{Total iterations per chain (half are warmup). Default 2000.}

\item{seed}{Sampler seed.}
}
\value{
Named list with \verb{$success}, \verb{$z}, \verb{$CTmax_1hr}, \verb{$T_crit},
\verb{$draws} (per-draw posterior of all three quantities, as a
data frame with columns \code{.draw}, \code{z}, \code{CTmax_1hr}, \code{T_crit}),
and \verb{$diagnostics}.
}
\description{
Thin wrapper around \code{\link[=standardize_data]{standardize_data()}} + \code{\link[=fit_4pl]{fit_4pl()}} + \code{\link[=extract_tdt]{extract_tdt()}}
configured for simulation speed: 2 chains, 2000 iterations (warmup 1000),
minimal console output, no on-disk caching. Returns the same three-piece
summary shape as \code{\link[=fit_two_stage_classical]{fit_two_stage_classical()}} so the two estimators can be
compared row-for-row, \strong{plus} the full per-draw posterior of $z$,
$CT_{max_{1hr}}$, and $T_{crit}$ (length \code{ndraws} each). The per-draw
vectors let downstream code pool posteriors across simulations or recompute
alternative summaries without re-fitting.
}
