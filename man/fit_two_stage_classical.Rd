% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_twostage_helpers.R
\name{fit_two_stage_classical}
\alias{fit_two_stage_classical}
\title{Classical two-stage TDT estimator (logit-GLM per temperature + OLS)}
\usage{
fit_two_stage_classical(data, t_ref_min = 60)
}
\arguments{
\item{data}{Output of \code{\link[=sim_twostage_dataset]{sim_twostage_dataset()}} (raw simulated dataset).}

\item{t_ref_min}{Reference exposure time, in minutes. Default 60.}
}
\value{
Named list. \verb{$success} is \code{FALSE} if Stage 1 failed for any
temperature (degenerate cells or non-finite LT50). Otherwise:
\verb{$z} and \verb{$CTmax_1hr} each as \code{list(point, lower, upper)};
\verb{$stage1} (per-temperature LT50 estimates).
}
\description{
Stage 1: for each temperature, fits a binomial GLM with logit link on
survival ~ log10(duration); reads off $\log_{10}\text{LT50}$ at $\hat p = 0.5$.
Stage 2: OLS of $\log_{10}\text{LT50}$ on temperature; reports $z = -1/\hat\beta_1$
and $CT_{max_{1hr}} = (\log_{10} 60 - \hat\beta_0)/\hat\beta_1$.
}
\details{
95\% CIs come from the delta method on the Stage-2 coefficients. This is the
implementation field practice uses; alternative weighting (1/SE^2) at
Stage 2 is intentionally not used here so the simulation benchmarks the
pipeline as published.
}
