% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_surv_draws}
\alias{get_surv_draws}
\title{Posterior draws of survival}
\usage{
get_surv_draws(x)
}
\arguments{
\item{x}{A list returned by \code{\link[=predict_survival_curves]{predict_survival_curves()}} or by
\code{\link[=predict_heat_injury]{predict_heat_injury()}}.}
}
\value{
A long-format tibble. For \code{\link[=predict_survival_curves]{predict_survival_curves()}}: \code{.draw},
\code{temp}, \code{duration}, \code{survival}. For \code{\link[=predict_heat_injury]{predict_heat_injury()}}:
\code{.draw}, \code{time_h}, \code{temp}, \code{survival}.
}
\description{
Accepts either a \code{\link[=predict_survival_curves]{predict_survival_curves()}} result (static survival on a
temperature × duration grid) or a \code{\link[=predict_heat_injury]{predict_heat_injury()}} result (dynamic
survival along a temperature trace). In the heat-injury case requires
\code{save_draws = TRUE}.
}
\examples{
\dontrun{
psc <- predict_survival_curves(wf, temps = c(32, 34),
                               durations = c(0.5, 1))
get_surv_draws(psc)

hi <- predict_heat_injury(trace, wf, save_draws = TRUE)
get_surv_draws(hi)
}
}
