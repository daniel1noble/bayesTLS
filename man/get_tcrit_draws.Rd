% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_tcrit_draws}
\alias{get_tcrit_draws}
\title{Posterior draws of T_crit (rate-multiplier definition)}
\usage{
get_tcrit_draws(et)
}
\arguments{
\item{et}{The list returned by \code{extract_tdt(..., lethal = TRUE)}.}
}
\value{
A tibble with columns \code{.draw}, \code{T_crit}, and \code{log10_rate} (the
sampled log10 rate-multiplier for that posterior draw).
}
\description{
Pulls the per-draw T_crit values from an \code{\link[=extract_tdt]{extract_tdt()}} result. Errors if
\code{extract_tdt()} was called with the default \code{lethal = FALSE} — T_crit is
only meaningful for lethal-endpoint data.
}
\examples{
\dontrun{
get_tcrit_draws(extract_tdt(wf, lethal = TRUE))
}
}
\seealso{
\code{\link[=get_tcrit_summary]{get_tcrit_summary()}} for the median + 95\% credible-interval summary.
}
