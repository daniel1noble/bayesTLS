% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_tcrit_summary}
\alias{get_tcrit_summary}
\title{Posterior summary of T_crit (rate-multiplier definition)}
\usage{
get_tcrit_summary(et)
}
\arguments{
\item{et}{The list returned by \code{extract_tdt(..., lethal = TRUE)}.}
}
\value{
A tibble with columns \code{TC_rate_low}, \code{TC_rate_high}, \code{temp_lower},
\code{temp_median}, \code{temp_upper}.
}
\description{
Pulls the T_crit summary table from an \code{\link[=extract_tdt]{extract_tdt()}} result — median and
credible interval, plus the rate-floor range used — as opposed to the
per-draw values returned by \code{\link[=get_tcrit_draws]{get_tcrit_draws()}}. Errors if \code{extract_tdt()}
was called with the default \code{lethal = FALSE}; T_crit is only meaningful for
lethal-endpoint data.
}
\examples{
\dontrun{
get_tcrit_summary(extract_tdt(wf, lethal = TRUE))
}
}
\seealso{
\code{\link[=get_tcrit_draws]{get_tcrit_draws()}} for the per-draw posterior.
}
