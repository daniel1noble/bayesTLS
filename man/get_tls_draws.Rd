% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_tls_draws}
\alias{get_tls_draws}
\title{Paired posterior draws of z, CTmax and (optionally) T_crit}
\usage{
get_tls_draws(et)
}
\arguments{
\item{et}{The list returned by \code{\link[=extract_tdt]{extract_tdt()}}.}
}
\value{
A tibble with one row per shared posterior draw: columns \code{.draw}, the
moderator column(s) for a grouped fit, \code{z}, \code{CTmax}, and (when available)
\code{T_crit}. The auxiliary \code{log10_rate} from \code{\link[=get_tcrit_draws]{get_tcrit_draws()}} is dropped.
}
\description{
Merges the per-quantity draws from an \code{\link[=extract_tdt]{extract_tdt()}} result into a single
tibble, joined on \code{.draw} (and the moderator column(s) for a grouped fit) so
that every quantity is on the \strong{same posterior draw}. This preserves their
joint correlation, which is what per-draw contrasts, ratios and bivariate
plots need. \code{z} and \code{CTmax} are always returned; \code{T_crit} is included only
when \code{\link[=extract_tdt]{extract_tdt()}} was called with \code{lethal = TRUE}.
}
\details{
The merge is an inner join, not a column-bind: each quantity is filtered to
finite values independently inside \code{\link[=extract_tdt]{extract_tdt()}} (e.g. a draw with a
near-zero midpoint slope has no finite \code{z}; a draw whose curve never crosses
the reference duration has no finite \code{CTmax}), so the draws can differ in
which rows they retain. Joining on \code{.draw} keeps only draws present for every
returned quantity and guarantees alignment; binding columns could silently
mis-pair them.
}
\examples{
\dontrun{
wf <- fit_4pl(std)
d  <- get_tls_draws(extract_tdt(wf, lethal = TRUE))
# joint per-draw transform keeps z and CTmax paired:
stats::quantile(d$CTmax - 2.5 * d$z, c(0.025, 0.5, 0.975))
}
}
\seealso{
\code{\link[=get_z_draws]{get_z_draws()}}, \code{\link[=get_ctmax_draws]{get_ctmax_draws()}}, \code{\link[=get_tcrit_draws]{get_tcrit_draws()}} for the
individual per-quantity draws; \code{\link[=extract_tdt]{extract_tdt()}}.
}
