% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_tls_est}
\alias{get_tls_est}
\title{Extract TLS estimates (draws or summary) from a \code{tls} object}
\usage{
get_tls_est(x, what = c("summary", "draws"), params = NULL)
}
\arguments{
\item{x}{A \code{tls} object, the result of \code{\link[=tls]{tls()}}.}

\item{what}{Either \code{"summary"} (the default; median + lower/upper per
quantity x group) or \code{"draws"} (one row per posterior draw per quantity x
group).}

\item{params}{Optional character vector selecting quantities to return, matched
case-insensitively against \code{"z"}, \code{"CTmax"}, \code{"Tcrit"}. \code{NULL} (default)
returns all available quantities.}
}
\value{
A tibble. For \code{what = "summary"}: columns \code{quantity}, \code{median},
\code{lower}, \code{upper} (plus grouping columns for a grouped fit). For
\code{what = "draws"}: columns \code{quantity}, \code{.draw}, \code{value} (plus grouping
columns).
}
\description{
Pulls the posterior \code{draws} or the \code{summary} (median + 95\% credible interval)
for any or all thermal-load-sensitivity quantities (\code{z}, \code{CTmax}, and, when the
fit was extracted with \code{lethal = TRUE}, \code{Tcrit}) from a \code{\link[=tls]{tls()}} result. This is
the accessor companion for \code{tls} objects, mirroring \code{\link[=get_tls_summary]{get_tls_summary()}} /
\code{\link[=get_tls_draws]{get_tls_draws()}} for an \code{\link[=extract_tdt]{extract_tdt()}} result. Draws are returned in
tidy-long form (\code{quantity}, \code{.draw}, \code{value}, plus any grouping column), so a
posterior contrast is a join on \code{.draw} followed by a difference of \code{value}.
}
\examples{
\dontrun{
fit <- fit_4pl(standardize_data(my_data, temp = "temp", duration = "time",
                                n_total = "n", n_dead = "dead"))
est <- tls(fit, lethal = TRUE)
get_tls_est(est, "summary")             # all quantities: median + 95\% CrI
zd  <- get_tls_est(est, "draws", "z")   # z draws, ready for a contrast
}
}
\seealso{
\code{\link[=tls]{tls()}}; \code{\link[=get_tls_summary]{get_tls_summary()}} and \code{\link[=get_tls_draws]{get_tls_draws()}} for an
\code{\link[=extract_tdt]{extract_tdt()}} result.
}
