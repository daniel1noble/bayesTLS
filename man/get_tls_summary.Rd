% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_tls_summary}
\alias{get_tls_summary}
\title{Combined posterior summary of z, CTmax and (optionally) T_crit}
\usage{
get_tls_summary(et)
}
\arguments{
\item{et}{The list returned by \code{\link[=extract_tdt]{extract_tdt()}}.}
}
\value{
A tibble with columns \code{quantity} (\code{"z"}, \code{"CTmax"}, and when available
\code{"Tcrit"}), \code{median}, \code{lower}, \code{upper}, plus the moderator column(s) for a
grouped fit. The \code{T_crit} rate-floor columns (\verb{TC_rate_*}) are dropped.
}
\description{
Reshapes the per-quantity summaries from an \code{\link[=extract_tdt]{extract_tdt()}} result into one
tidy-long tibble — the summary companion to \code{\link[=get_tls_draws]{get_tls_draws()}}, and the same
shape \code{\link[=tls]{tls()}} returns: one row per quantity (per group, for a grouped fit),
with harmonised \code{median} / \code{lower} / \code{upper} columns. \code{z} and \code{CTmax} are
always returned; \code{Tcrit} is included only when \code{\link[=extract_tdt]{extract_tdt()}} was called
with \code{lethal = TRUE}. (The individual accessors use quantity-specific column
names — \code{z_median} vs \code{temp_median} — so this is the tidy way to tabulate or
plot all quantities together.)
}
\examples{
\dontrun{
get_tls_summary(extract_tdt(wf, lethal = TRUE))
}
}
\seealso{
\code{\link[=get_tls_draws]{get_tls_draws()}} for the paired per-draw posterior;
\code{\link[=get_z_summary]{get_z_summary()}}, \code{\link[=get_ctmax_summary]{get_ctmax_summary()}}, \code{\link[=get_tcrit_summary]{get_tcrit_summary()}} for the
individual summaries; \code{\link[=tls]{tls()}}, which returns the same tidy shape from a fit.
}
