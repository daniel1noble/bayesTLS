% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_z_draws}
\alias{get_z_draws}
\title{Posterior draws of z (thermal sensitivity, °C per 10-fold change in time)}
\usage{
get_z_draws(et)
}
\arguments{
\item{et}{The list returned by \code{\link[=extract_tdt]{extract_tdt()}}.}
}
\value{
A tibble with columns \code{.draw} and \code{z} (plus the moderator column(s)
for a grouped fit).
}
\description{
Pulls the per-draw values of z from an \code{\link[=extract_tdt]{extract_tdt()}} result.
}
\examples{
\dontrun{
et_a <- extract_tdt(wf_group_a)
et_b <- extract_tdt(wf_group_b)
contrast <- merge(get_z_draws(et_a), get_z_draws(et_b),
                  by = ".draw", suffixes = c("_a", "_b"))
contrast$diff <- contrast$z_a - contrast$z_b
quantile(contrast$diff, c(0.025, 0.5, 0.975))
}
}
\seealso{
\code{\link[=get_z_summary]{get_z_summary()}} for the median + 95\% credible-interval summary.
}
