% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_heat_injury.R
\name{hi_local_curves}
\alias{hi_local_curves}
\title{Temperature-local 4PL curves per posterior draw (internal, for shape="varying")}
\usage{
hi_local_curves(workflow, by = NULL, temps_unique)
}
\arguments{
\item{workflow}{Fitted \code{bayes_tls}.}

\item{by}{Resolved moderator column(s), or \code{NULL} for a single-condition fit.}

\item{temps_unique}{Numeric vector of unique (uncentred °C) temperatures to
evaluate the curves at -- typically \code{unique(trace$temp)}.}
}
\value{
A named list, one element per moderator group (named by the group
label, \code{"all"} when ungrouped). Each element is a list with:
\code{temps} (the input temperatures, in evaluation order), \code{.draw} (the
posterior draw indices kept after filtering invalid draws), and the
matrices \code{low}, \code{up}, \code{k}, \code{mid}, each \verb{[length(.draw) × length(temps)]}.
A draw is kept only if ALL its cells are valid (finite, \code{k > 0},
\code{up > low}) at every temperature, so the integrator never hits a partial
draw.
}
\description{
Evaluates the full natural-scale 4PL \code{low(T)}, \code{up(T)}, \code{k(T)}, \code{mid(T)} per
posterior draw at a set of unique temperatures, retaining every sub-parameter's
temperature/group structure (the slopes the constant-shape \code{\link[=hi_pars]{hi_pars()}} view
drops). Returns per-draw × per-temperature matrices, keyed for fast lookup by
the shape-varying heat-injury integrator: each trace point's temperature maps
to a column, each posterior draw to a row.
}
\keyword{internal}
