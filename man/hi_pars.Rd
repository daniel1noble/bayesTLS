% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_heat_injury.R
\name{hi_pars}
\alias{hi_pars}
\title{Constant-shape 4PL parameters for the heat-injury integral (internal)}
\usage{
hi_pars(workflow, by = NULL)
}
\arguments{
\item{workflow}{Fitted \code{bayes_tls}.}

\item{by}{Resolved moderator column(s) (already through \code{tdt_resolve_by()}),
or \code{NULL} for a single-condition fit.}
}
\value{
A tibble with \verb{(.draw, low, up, k, mid_int, mid_temp)} (plus the
moderator column(s) for a grouped fit), filtered to valid draws.
}
\description{
The classical heat-injury integral is evaluated under the assumption that
the asymptotes (\code{low}, \code{up}) and steepness (\code{k}) are constant in
temperature and only the midpoint shifts. This helper returns exactly that
view, per posterior draw and per moderator group: \code{low}, \code{up}, \code{k} read at
the centring temperature (\code{temp_c = 0}, i.e. \code{T_bar}), plus a linear
midpoint decomposed into intercept (\code{mid_int}, at \code{T_bar}) and slope
(\code{mid_temp}, per °C). This is the per-draw input
\code{\link[=predict_heat_injury]{predict_heat_injury()}} uses for \code{shape = "constant"}.
}
\details{
Reads \code{posterior_linpred(nlpar=)} at \code{temp_c = 0} (asymptotes + midpoint
intercept) and \code{temp_c = 1} (for the linear midpoint slope), so it is
parameterisation- and coding-agnostic. If the temperature slopes on
\code{low}/\code{up}/\code{k} are shrunk near zero by the data this introduces no bias;
otherwise it is the same approximation the classical HI framework makes
(see \code{shape = "varying"} for the slope-aware alternative).
}
\keyword{internal}
