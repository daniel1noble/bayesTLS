% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{make_4pl_priors}
\alias{make_4pl_priors}
\title{Default weakly informative priors for the joint Bayesian 4PL}
\usage{
make_4pl_priors(
  data,
  lower = 0,
  upper = 1,
  random_effects = NULL,
  prior_random_sd = "exponential(2)",
  prior_phi = "gamma(2, 0.1)",
  bounds = NULL,
  ctmax = NULL,
  z = NULL,
  up = NULL,
  low = NULL,
  k = NULL
)
}
\arguments{
\item{data}{Output of \code{\link[=standardize_data]{standardize_data()}}. Only \code{logd} is used,
to centre the midpoint Intercept prior.}

\item{lower, upper}{Response-scale bounds for the asymptotes. Default
\verb{0, 1} for proportion data. For sublethal data
bounded above \code{0} (e.g. photosystem-II between 0.85
and 1), set \code{lower} accordingly.}

\item{random_effects}{Optional character vector of grouping variables for
random intercepts on \code{mid}. Adds one
\code{prior_random_sd} row per group.}

\item{prior_random_sd}{Stan prior string for random-effect SDs. Default
\code{"exponential(2)"}.}

\item{prior_phi}{Stan prior string for the beta-binomial precision
parameter \code{phi}. Default \code{"gamma(2, 0.1)"}. Pass
\code{NULL} to omit the \code{phi} prior — needed when fitting
with \code{family = binomial()} (no overdispersion).}

\item{bounds}{Length-2 \code{c(lower, upper)} asymptote interval;
supersedes \code{lower}/\code{upper} when supplied.}

\item{ctmax, z, up, low, k}{One-sided formulas selecting the \strong{direct} CTmax/z
priors (matching \code{\link[=make_4pl_formula]{make_4pl_formula()}}). Supplying
\code{ctmax}/\code{z} switches to direct-mode priors: the same
centred asymptote priors (per factor level for
cell-means terms), coding-aware weakly-informative
\code{CTmaxdev}/\code{logz} priors (the level prior lands on the
Intercept or each factor level, while between-group
contrasts and temperature slopes are centred on zero, so
per-group z/CTmax are invariant to cell-means vs
treatment coding), and random-effect SD priors on
\code{CTmaxdev}/\code{logz} only (never on the shape
sub-parameters). \code{up}/\code{low}/\code{k}
follow the same inheritance resolution as the formula.}
}
\value{
A \code{brmsprior} object ready to pass to \code{\link[=fit_4pl]{fit_4pl()}}.
}
\description{
Priors are weakly informative and do not adapt to the data (which would
understate uncertainty). They are constructed to match
\code{\link[=make_4pl_formula]{make_4pl_formula()}}'s disjoint-bounds reparameterisation
\code{low = low_min + inv_logit(lowraw) * low_w} and
\code{up = up_min + inv_logit(upraw) * up_w} for the asymptote intervals derived
from \code{lower} and \code{upper} by \code{\link[=compute_4pl_bounds]{compute_4pl_bounds()}}.
}
\details{
Each of the four 4PL sub-parameters (\code{lowraw}, \code{upraw}, \code{logk}, \code{mid}) gets
an Intercept-specific prior plus a general \code{class = "b"} prior covering the
\code{temp_c} slope. Random-effect SDs on \code{mid} get one \code{prior_random_sd} row per
grouping variable.
}
\examples{
raw <- data.frame(
  temperature_C = rep(c(30, 32, 34), each = 4),
  exposure_h    = rep(c(1, 2, 4, 8), times = 3),
  n             = 30L,
  alive         = c(29, 28, 25, 5, 30, 27, 18, 2, 28, 22, 10, 1)
)
d <- standardize_data(raw, temp = "temperature_C", duration = "exposure_h",
                      n_total = "n", n_surv = "alive")
make_4pl_priors(d)
}
