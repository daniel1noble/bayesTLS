% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_survival_curves.R
\name{new_tdt_grid}
\alias{new_tdt_grid}
\title{Build a prediction grid for a fitted TDT workflow}
\usage{
new_tdt_grid(workflow, temps, durations, n_total = NULL)
}
\arguments{
\item{workflow}{A \code{bayes_tls} object.}

\item{temps}{Numeric vector of assay temperatures (°C).}

\item{durations}{Numeric vector of exposure durations (same unit as the
training data, typically hours).}

\item{n_total}{Integer trials count. \code{NULL} (default) uses the median of
the training data.}
}
\value{
A tibble with one row per \verb{(temp, duration)} pair.
}
\description{
Constructs a \code{newdata} tibble covering all combinations of \code{temps} and
\code{durations}, adds the derived \code{logd} and \code{temp_c} columns the model needs,
and fills random-effect grouping columns with their first observed level
(these are marginalised out at prediction time via \code{re_formula = NA}).
}
\keyword{internal}
