% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temperature_scenarios.R
\name{planted_dose_from_trace}
\alias{planted_dose_from_trace}
\title{Analytical heat-injury accumulation along a temperature trace}
\usage{
planted_dose_from_trace(trace, z, CTmax_1hr, T_c)
}
\arguments{
\item{trace}{Tibble with columns \code{time} and \code{temp}, output of
\code{\link[=make_temperature_scenarios]{make_temperature_scenarios()}}.}

\item{z}{Thermal sensitivity, °C per 10-fold change in time.}

\item{CTmax_1hr}{Static temperature at which LT50 = 1 hour, °C.}

\item{T_c}{Damage threshold (°C); contributions below \code{T_c} are zero.}
}
\value{
The input trace augmented with two new columns:
- \code{hi_inc} — instantaneous HI contribution at each hour (\%).
- \code{hi_cumulative} — running sum of \code{hi_inc} (\%).
}
\description{
Implements the classical HI integral exactly (Equation 7 of the manuscript),
given known TDT parameters. Use this as the \strong{truth} that
\code{\link[=predict_heat_injury]{predict_heat_injury()}} should recover when fed the same trace and a model
fit whose posterior is consistent with \verb{(z, CTmax_1hr, T_c)}.
}
\details{
At each time step, contribution is

\deqn{\Delta HI_i = 100 \cdot 10^{(T_i - CT_{max,1hr}) / z} \cdot \Delta t}

when \code{T_i > T_c}, and zero otherwise. Cumulative HI is the running sum.
}
\examples{
scens <- make_temperature_scenarios(spike_temp = 30)
planted_dose_from_trace(scens$single_spike, z = 5, CTmax_1hr = 32, T_c = 25)
}
