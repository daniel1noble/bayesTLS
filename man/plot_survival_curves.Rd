% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_survival_curves}
\alias{plot_survival_curves}
\title{Plot posterior survival curves}
\usage{
plot_survival_curves(
  pred,
  observed = NULL,
  log_time = FALSE,
  palette = "viridis",
  response_label = "Survival probability",
  clip_to_observed = TRUE,
  facet_scales = c("auto", "fixed", "free", "free_x", "free_y")
)
}
\arguments{
\item{pred}{Output of \code{\link[=predict_survival_curves]{predict_survival_curves()}}.}

\item{observed}{Optional standardised data tibble (from \code{\link[=standardize_data]{standardize_data()}})
to overlay as points (jittered, transparent).}

\item{log_time}{Logical. If \code{TRUE}, the exposure-duration axis is log10.
Default \code{FALSE} (linear).}

\item{palette}{Character. Viridis option for the temperature colour scale.
Default \code{"viridis"}; pass \code{NULL} to keep ggplot's default
hue scale.}

\item{response_label}{Character. y-axis label for the modelled response.
Default \code{"Survival probability"}; set to e.g.
\code{"Retained PSII function"} for a continuous-proportion (Beta)
endpoint where the response is not survival.}

\item{clip_to_observed}{Logical. If \code{TRUE} (the default) and \code{observed} is
supplied, each temperature/group curve is capped at the
longest exposure actually observed for that cell — shorter
durations are still predicted (the curve there is
well-behaved, rising to the upper asymptote), but the long tail
is not extrapolated past the data (e.g. designs that test much
shorter exposures at hotter temperatures). Prediction cells with
no matching observed temperature/group combination are dropped.
A no-op when \code{observed} is not supplied. Set \code{FALSE} for the
full grid.}

\item{facet_scales}{Facet scale behaviour for grouped fits. \code{"auto"} (default)
frees the duration (x) axis per group when
\code{clip_to_observed = TRUE} and \code{observed} is supplied, so each
group fills its own tested range (survival stays shared at
\verb{[0, 1]}); otherwise fixed. Or pass any \code{facet_wrap()} scale
(\code{"fixed"}, \code{"free"}, \code{"free_x"}, \code{"free_y"}).}
}
\value{
A ggplot object.
}
\description{
Plots posterior median ± 95\% CrI of survival as a function of duration, one
coloured line per assay temperature. Optionally overlays observed survival
proportions per replicate.
}
\details{
Colour mapping uses the viridis palette by default (yellow at the highest
temperature, dark purple at the lowest); the exposure-duration axis is
linear by default, with the classical log-time TDT view available via
\code{log_time = TRUE}.
}
\examples{
\dontrun{
pred <- predict_survival_curves(wf, temps = c(30, 32, 34, 36))
plot_survival_curves(pred, observed = wf$data)
plot_survival_curves(pred, log_time = TRUE)  # classical TDT view
}
}
