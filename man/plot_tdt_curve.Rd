% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_tdt_curve}
\alias{plot_tdt_curve}
\title{Plot a posterior LT_x curve}
\usage{
plot_tdt_curve(ltx, panels = c("both", "linear", "log"), colour = "#146C7C")
}
\arguments{
\item{ltx}{Output of \code{\link[=derive_tdt_curve]{derive_tdt_curve()}}.}

\item{panels}{One of \code{"both"} (default), \code{"linear"}, or \code{"log"}.}

\item{colour}{Line/ribbon colour. Default \code{"#146C7C"}.}
}
\value{
A ggplot object (single panel) or patchwork composition (two panels).
}
\description{
Plots posterior median +/- 95\% CrI of the duration to reach \code{target_surv}
across temperature. By default, returns a two-panel figure: a linear-time
panel (the absolute time to reach the target) alongside a \code{log10}-time
panel (the classical TDT view where the relationship is near-linear with
slope \code{-1/z}). Both panels share posterior draws.
}
\details{
Internal y-axis behaviour: if the 95\% credible interval of the LT_x curve
extends above 48 hours (converted to \code{ltx$output_time_unit}), both panels'
y-axes are clamped to 48 hours so the readable part of the curve isn't
compressed by an extreme upper tail at cool temperatures. If the data
already stay below 48 hours, no limits are applied and \code{ggplot2} chooses
a data-driven range. Callers can override either way by appending their
own \code{+ ggplot2::coord_cartesian(ylim = ...)} to the returned plot.
}
\examples{
\dontrun{
ltx <- derive_tdt_curve(wf, temp_grid = seq(29, 37, by = 0.5))
plot_tdt_curve(ltx)                  # two-panel default
plot_tdt_curve(ltx, panels = "log")  # classical TDT view only

# Override the internal 48-h cap by appending a coord_cartesian:
plot_tdt_curve(ltx) +
  ggplot2::coord_cartesian(ylim = c(0, 240))
}
}
