% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_tdt_landscape}
\alias{plot_tdt_landscape}
\title{Plot the TDT survival landscape as a heatmap}
\usage{
plot_tdt_landscape(
  landscape,
  observed = NULL,
  contours = c(0.25, 0.5, 0.75),
  log_time = FALSE,
  clip_to_observed = TRUE
)
}
\arguments{
\item{landscape}{Output of \code{\link[=derive_tdt_landscape]{derive_tdt_landscape()}}.}

\item{observed}{Optional standardised data tibble to overlay.}

\item{contours}{Numeric vector of survival levels to draw contours at.
Default \code{c(0.25, 0.5, 0.75)}.}

\item{log_time}{Logical. If \code{TRUE}, exposure-duration y-axis is log10.
Default \code{FALSE} (linear).}

\item{clip_to_observed}{Logical. If \code{TRUE} (the default) and \code{observed} is
supplied, each group's heatmap is restricted to the observed
temperature and exposure-duration extent (a per-group
bounding box), so the surface is not drawn beyond the data.
A no-op when \code{observed} is not supplied. Set \code{FALSE} for the
full grid.}
}
\value{
A ggplot object.
}
\description{
Exposure-duration axis is linear by default; pass \code{log_time = TRUE} for the
classical log-time TDT presentation.
}
\details{
A grouped fit (a moderator on \code{CTmax}/\code{z}, e.g. \code{oxygen} or \code{species})
produces one heatmap panel per group, mirroring \code{\link[=plot_survival_curves]{plot_survival_curves()}};
a single-condition fit returns the one ungrouped heatmap.
}
\examples{
\dontrun{
lsp <- derive_tdt_landscape(wf)
plot_tdt_landscape(lsp, observed = wf$data)
}
}
