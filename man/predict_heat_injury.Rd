% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_heat_injury.R
\name{predict_heat_injury}
\alias{predict_heat_injury}
\title{Predict heat injury and survival under a fluctuating temperature trace}
\usage{
predict_heat_injury(
  trace,
  workflow,
  target_surv = "relative",
  T_c = NULL,
  trace_unit = "hours",
  ndraws = NULL,
  repair = FALSE,
  repair_pars = NULL,
  repair_scales_with_survival = TRUE,
  irreversible_mortality = TRUE,
  save_draws = FALSE,
  seed = NULL,
  by = NULL
)
}
\arguments{
\item{trace}{Tibble with columns \code{time} (numeric time from start,
in \code{trace_unit}) and \code{temp} (°C), in time order.
Requires ≥ 2 rows.}

\item{workflow}{Fitted \code{bayes_tls}.}

\item{target_surv}{Threshold defining "1 dose". \code{"relative"} (default;
\code{(low + up)/2}), \code{"absolute"} (= 0.5), or a numeric in
\verb{(0, 1)}. The default coincides with the classical LT50
when \verb{low ≈ 0} and \verb{up ≈ 1}; with sub-unit asymptotes
the relative threshold is the more biologically
meaningful anchor for dose accounting.}

\item{T_c}{Optional damage-accumulation threshold (°C). When
supplied, the damage rate is forced to zero at
\code{temp <= T_c} (matches Equation 7 of the manuscript).
Default \code{NULL} lets the rate fall naturally with T.}

\item{trace_unit}{Time unit of the trace's \code{time} column: one of
\code{"hours"} (default), \code{"minutes"}, \code{"seconds"}, \code{"days"}.
Reconciled internally with the model's fitted
\code{duration_unit}, so the result is correct for any
combination of model and trace time units.}

\item{ndraws}{Posterior draws to use; \code{NULL} (default) uses the full
posterior, keeping the integral consistent with the model.
Pass an integer to subsample -- useful for speed, since the
full posterior over a long temperature trace can be slow.}

\item{repair}{Logical. If \code{TRUE}, add Sharpe-Schoolfield repair.
Default \code{FALSE}.}

\item{repair_pars}{Required when \code{repair = TRUE}. Named list with elements
\verb{TA, TAL, TAH, TL, TH, TREF, r_ref} passed straight to
\code{\link[=repair_rate_schoolfield]{repair_rate_schoolfield()}}. \code{r_ref} should be in
"doses per hour" so it matches the damage-rate units.}

\item{repair_scales_with_survival}{Logical. If \code{TRUE} (default), repair
rate at each step is scaled by \code{survival / up} so dead
organisms do not contribute to repair.}

\item{irreversible_mortality}{Logical. If \code{TRUE} (default), survival can
only decrease over time — i.e. once the population's
predicted survival reaches a value, it cannot rebound
even if cumulative dose subsequently decreases.}

\item{save_draws}{Logical. If \code{TRUE}, return the full per-draw
trajectories. Default \code{FALSE}.}

\item{seed}{Optional integer seeding the posterior-draw subsample for
reproducibility. \code{NULL} (default) leaves the RNG untouched.}

\item{by}{Optional moderator column(s) for per-group injury. \code{NULL}
(default) uses the fit's moderators; a single-condition fit
returns the ungrouped result. A grouped fit runs the dose
integral through each group's 4PL and \code{summary} gains the
moderator column(s).}
}
\value{
A list with elements:
\itemize{
\item \code{summary}: tibble with \code{time}, \code{temp}, and posterior median + 95\%
CrI for \code{hi}, \code{survival}, and \code{mortality} at each time step (plus the
moderator column(s) for a grouped fit).
\item \code{draws}: optional per-draw trajectories (when \code{save_draws = TRUE}).
\item \code{meta}: list of inputs used.
}
}
\description{
Propagates the model's full posterior through an Eulerian damage-
accumulation integration along the supplied temperature trace, returning
the posterior median and 95\% credible band of:
}
\details{
\itemize{
\item \strong{HI(t)} — cumulative heat injury, in percent of an LT_target_surv dose.
When \code{HI(t) = 100}, the population has accumulated one full dose at the
chosen survival threshold (default 50\% mortality).
\item \strong{S(t)} — predicted survival fraction, mapped from the cumulative dose
through the fitted 4PL.
}

Optionally adds a temperature-dependent repair rate via
\code{\link[=repair_rate_schoolfield]{repair_rate_schoolfield()}}.
}
\examples{
\dontrun{
scens <- make_temperature_scenarios()
hi    <- predict_heat_injury(scens$single_spike, wf)
hi$summary
}
}
