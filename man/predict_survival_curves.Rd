% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_survival_curves.R
\name{predict_survival_curves}
\alias{predict_survival_curves}
\title{Posterior survival curves on a temperature × duration grid}
\usage{
predict_survival_curves(
  workflow,
  temps = NULL,
  durations = NULL,
  ndraws = 1000,
  probs = c(0.025, 0.5, 0.975)
)
}
\arguments{
\item{workflow}{A fitted \code{tdt_4pl_workflow}.}

\item{temps}{Numeric vector of temperatures (°C). Default: unique assay
temperatures in the training data.}

\item{durations}{Numeric vector of durations. Default: 250 log-spaced values
spanning 0.2× to 5× the observed range.}

\item{ndraws}{Integer number of posterior draws to use. Default 1000.}

\item{probs}{Numeric length-3 quantile probabilities (lower, median,
upper). Default \code{c(0.025, 0.5, 0.975)}.}
}
\value{
A list with elements \code{summary} (tibble of \code{temp}, \code{duration},
\code{survival_median}, \code{survival_lower}, \code{survival_upper}) and \code{draws}
(the raw posterior matrix as a long tibble).
}
\description{
Predicts survival probability at every combination of \code{temps} × \code{durations}
under a fitted 4PL, then returns the posterior median and 95\% credible
interval at each grid point. Random effects are marginalised out.
}
\details{
Use this for \emph{curves} (a handful of temperatures, dense in duration). For a
2-D survival heatmap call \code{\link[=derive_tdt_landscape]{derive_tdt_landscape()}} instead.
}
\examples{
\dontrun{
wf <- fit_4pl(d, ...)
pred <- predict_survival_curves(wf, temps = c(30, 32, 34))
}
}
