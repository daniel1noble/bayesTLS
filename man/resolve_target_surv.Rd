% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_tdt.R
\name{resolve_target_surv}
\alias{resolve_target_surv}
\title{Normalise a \code{target_surv} argument}
\usage{
resolve_target_surv(target_surv)
}
\description{
Accepts the user-facing argument (string \code{"relative"}/\code{"absolute"} or
numeric in \verb{(0, 1)}) and returns a list describing the chosen threshold
mode plus a character label suitable for embedding in result tibbles.
}
\details{
\itemize{
\item \code{"relative"} (default) → threshold = \code{(low + up)/2} per posterior draw.
The 4PL \code{mid} parameter is the log10-time at this threshold, so no
numerical inversion is needed.
\item \code{"absolute"} → threshold = 0.5 (literal survival probability).
\item numeric \code{p} in \verb{(0, 1)} → threshold = \code{p} (literal survival probability).
}
}
\keyword{internal}
