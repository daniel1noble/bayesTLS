% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_survival_curves.R
\name{summarise_observed_survival}
\alias{summarise_observed_survival}
\title{Summarise observed survival counts to mean ± SE per (temp, duration) cell}
\usage{
summarise_observed_survival(observed)
}
\arguments{
\item{observed}{A tibble from \code{\link[=standardize_data]{standardize_data()}}.}
}
\value{
A tibble with columns \code{temp}, \code{duration}, \code{survival_mean},
\code{survival_se}, \code{survival_lower}, \code{survival_upper}, \code{n_units},
\code{n_total_sum}.
}
\description{
Useful as an overlay on plotted posterior curves. Returns one row per
temperature × duration combination, with the mean observed survival
proportion across replicates and a standard error.
}
\examples{
raw <- data.frame(T = rep(30, 6), hrs = rep(c(1, 5), each = 3),
                  n = 30, alive = c(28, 27, 29, 10, 12, 8))
d <- standardize_data(raw, "T", "hrs", n_total = "n", n_surv = "alive")
summarise_observed_survival(d)
}
