% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes_tls_methods.R
\name{summary.bayes_tls}
\alias{summary.bayes_tls}
\title{Summarise a fitted \code{bayes_tls} workflow}
\usage{
\method{summary}{bayes_tls}(object, ...)
}
\arguments{
\item{object}{A fitted \code{bayes_tls} workflow.}

\item{...}{Passed through to \code{\link[brms:summary.brmsfit]{brms::summary.brmsfit()}} (e.g.
\code{prob}, \code{mc_se}, \code{priors}, \code{robust}).}
}
\value{
A brms \code{brmssummary} object (brms handles printing).
}
\description{
Delegates to \code{summary()} on the underlying \code{brmsfit} (via
\code{\link[=get_brmsfit]{get_brmsfit()}}), so the returned object is a brms \code{brmssummary} with the
population-level coefficient table, group-level standard deviations,
family parameters, and HMC diagnostics already laid out by brms.
}
\details{
The high-level workflow context (data shape, centred-temperature mean,
asymptote bounds, random-effect grouping, draw count) is available via
\code{\link[=print.bayes_tls]{print.bayes_tls()}}. For natural-scale 4PL parameters (\code{low}, \code{up},
\code{k}, \code{z}), use \code{\link[=tdt_parameter_table]{tdt_parameter_table()}}. For the TDT quantities
(\code{z}, \code{CTmax_1hr}, optionally \code{T_crit}), use \code{\link[=extract_tdt]{extract_tdt()}}.
}
\examples{
\dontrun{
summary(wf)
summary(wf, prob = 0.9, robust = TRUE)
}
}
