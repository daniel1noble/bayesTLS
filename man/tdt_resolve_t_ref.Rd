% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{tdt_resolve_t_ref}
\alias{tdt_resolve_t_ref}
\title{Resolve the CTmax reference exposure for the TDT extractors}
\usage{
tdt_resolve_t_ref(t_ref, meta)
}
\arguments{
\item{t_ref}{Caller-supplied reference exposure (minutes) or \code{NULL}.}

\item{meta}{A \code{bayes_tls} workflow's \code{meta} list (uses \code{t_ref}).}
}
\value{
Numeric reference exposure in minutes.
}
\description{
\code{t_ref} is the reference exposure that \code{CTmax} is reported at. Because
\code{CTmax} shifts with the reference time (by \eqn{z \cdot \log_{10}} of the
ratio), reading a fit at a different \code{t_ref} than it was fitted with silently
moves \code{CTmax} (while leaving \code{z}, which is reference-invariant, unchanged).
When the caller does not supply \code{t_ref}, inherit the fit's own reference
(\code{meta$t_ref}, in minutes) and announce it via a message; fall back to 60 min
for raw fits that record no reference.
}
\keyword{internal}
