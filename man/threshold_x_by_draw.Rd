% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_survival_curves.R
\name{threshold_x_by_draw}
\alias{threshold_x_by_draw}
\title{Per-draw threshold inversion of a posterior-prediction matrix}
\usage{
threshold_x_by_draw(pred_mat, x, target)
}
\arguments{
\item{pred_mat}{Numeric matrix \verb{[ndraws × ngrid]} of survival probabilities.}

\item{x}{Numeric vector of grid values along which to invert
(length = \code{ncol(pred_mat)}).}

\item{target}{Numeric scalar: the probability value to invert at.}
}
\value{
Numeric vector of length \code{nrow(pred_mat)}.
}
\description{
Internal helper. For each row (draw) of \code{pred_mat}, finds the value of \code{x}
at which the predicted probability crosses \code{target}, via linear
interpolation. Returns one value per draw.
}
\details{
\code{stats::approx()} requires its \code{x} argument (here, the survival values) to
be monotonically increasing. Because the 4PL is monotonically \emph{decreasing}
in duration (and in temperature for a fixed duration), we sort by \code{p}
ascending before calling \code{approx()}.
}
\keyword{internal}
