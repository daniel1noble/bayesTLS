% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tls.R
\name{tls}
\alias{tls}
\alias{tls_z}
\alias{tls_ctmax}
\alias{tls_tcrit}
\title{Thermal load sensitivity summaries from any fitted 4PL model}
\usage{
tls(
  object,
  by = NULL,
  params = "all",
  target_surv = "relative",
  t_ref = 60,
  time_multiplier = NULL,
  lethal = FALSE,
  TC_rate_range = c(0.1, 1),
  temp = "temp_c",
  temp_mean = NULL,
  temp_grid = NULL,
  re_formula = NA,
  lower = 0,
  upper = 1,
  nlpars = c("lowraw", "upraw", "logk", "mid"),
  ndraws = NULL,
  newdata = NULL,
  probs = c(0.025, 0.5, 0.975),
  seed = NULL,
  mode = NULL,
  p = NULL
)

tls_z(object, ...)

tls_ctmax(object, ...)

tls_tcrit(object, ...)
}
\arguments{
\item{object}{A fitted \code{bayes_tls} workflow or a \code{brmsfit} 4PL whose
non-linear parameters are the (reparameterised) asymptotes, steepness and
midpoint.}

\item{by}{Character vector of moderator columns defining the groups reported
separately (e.g. \code{"sex"}). \code{NULL} (default) pools to a single group.}

\item{params}{Quantities to return: \code{"all"} (z, CTmax, and T_crit when
\code{lethal = TRUE}), or a subset of \code{c("z", "ctmax", "tcrit")}.}

\item{target_surv}{Survival threshold the LT curve is read at, shared with
\code{\link[=extract_tdt]{extract_tdt()}}: \code{"relative"} (default; the per-draw midpoint
\code{(low + up)/2}), \code{"absolute"} (literal LT50), or a numeric \code{p} in \verb{(0, 1)}
for an absolute LT\code{p}. The default is \code{"relative"} so \code{tls()} and
\code{\link[=extract_tdt]{extract_tdt()}} return the same z/CTmax for a given fit.}

\item{t_ref}{Reference exposure duration for \code{CTmax}, in minutes. Default 60.
Converted to the model's time scale via \code{time_multiplier}.}

\item{time_multiplier}{Multiplier from the model's time unit to minutes.
\code{NULL} (default) derives it from a \code{bayes_tls} workflow's \code{duration_unit}
(e.g. 60 for an hours model); a raw \code{brmsfit} with no metadata defaults to
1, so \code{t_ref} is then in the model's own time units.}

\item{lethal}{Logical; with \code{TRUE}, T_crit (rate-multiplier) is available.
T_crit is meaningful only for damage-accumulation (lethal) endpoints.}

\item{TC_rate_range}{Length-2 HI-rate floor range (\% per hour) for T_crit.}

\item{temp}{Name of the centred temperature column. Default \code{"temp_c"}.}

\item{temp_mean}{Centring constant mapping \code{temp} back to temperature, needed
for \code{CTmax}/\code{T_crit}. Taken from a \code{bayes_tls} workflow's metadata when
available; supply it for a raw \code{brmsfit}.}

\item{temp_grid}{Temperatures (on the \code{temp} scale) at which the LT curve is
evaluated for the slope/crossing. Default: 11 points over the observed range.}

\item{re_formula}{Passed to \code{\link[brms:posterior_linpred.brmsfit]{brms::posterior_linpred()}}; default \code{NA}
(population level). Include group-level terms (e.g. clone) for per-group draws.}

\item{lower, upper}{Response bounds of the disjoint-bounds reparameterisation.}

\item{nlpars}{Non-linear parameter names, ordered low / up / k / mid (raw scale).}

\item{ndraws}{Optional number of posterior draws to subsample.}

\item{newdata}{Optional explicit moderator x temperature grid; overrides the
\code{by}/\code{temp_grid} construction.}

\item{probs}{Summary quantiles (lower, median, upper). Default \code{c(.025,.5,.975)}.}

\item{seed}{Optional integer seeding the draw subsample (\code{ndraws}) and the
T_crit rate draws for reproducibility. \code{NULL} (default) leaves the RNG alone.}

\item{mode, p}{\strong{Deprecated}, superseded by \code{target_surv}. If supplied,
\code{mode = "relative"} maps to \code{target_surv = "relative"} and
\code{mode = "absolute"} (with \code{p}) to \code{target_surv = p}. Kept for back-compat.}

\item{...}{Additional arguments passed on to \code{\link[=tls]{tls()}} (used by the
\code{tls_z()}, \code{tls_ctmax()} and \code{tls_tcrit()} convenience wrappers).}
}
\value{
A \code{tls} object: \verb{$summary} (per-group, per-quantity median + interval),
\verb{$draws} (per-group, per-quantity posterior draws), and \verb{$meta}.
}
\description{
One call that derives the classical TDT quantities — thermal sensitivity
\code{z}, \code{CTmax}, and (for lethal endpoints) \code{T_crit} — \strong{per moderator group}
from a fitted joint 4PL, including hand-written \code{brms} models with moderators
(sex, life stage, clone, ...) on any sub-parameter. The four sub-parameters
are evaluated at a moderator x temperature grid with
\code{\link[brms:posterior_linpred.brmsfit]{brms::posterior_linpred()}}, then \code{z}, \code{CTmax} and \code{T_crit} are derived from
the same posterior draws (so they are mutually consistent and \code{T_crit} pairs
\code{CTmax} and \code{z} per draw).
}
\details{
\strong{Scope vs \code{\link[=extract_tdt]{extract_tdt()}}.} \code{tls()} derives \code{z}/\code{CTmax} from the
least-squares slope of the per-draw log10(LT)-versus-temperature line across
\code{temp_grid}, i.e. a regression-slope summary of the LT curve. With
\code{target_surv = "absolute"} (or a numeric \code{p}) the underlying LT curve can be
bent, and this slope summary then differs from \code{\link[=extract_tdt]{extract_tdt()}}, which uses a
local-\code{z} pooling / true inversion of the curve. The two engines coincide
only in the relative/linear case (\code{target_surv = "relative"}, the default),
where log10(LT) is linear in temperature; treat the absolute-mode \code{tls()}
output as a linear approximation, not as an inversion-based estimate.
}
\examples{
\dontrun{
tls(joint_sex_fit, by = "sex", lethal = TRUE, temp_mean = 36.1)  # z, CTmax, T_crit per sex
tls(wf_leaf, params = "z")                                       # z only, workflow
}
}
